{% extends 'layouts/layout.html.twig' %}


{% block content %}
    <div id="page-404" class="page page-404">
        {# HERO #}
        <header id="hero" class="text-center">
            {% if heroImage is defined and heroImage is not null %}
                {% set heroImage = heroImage.thumbnail('heroImage').html({
                    'imgAttributes': {
                        'class': 'd-none',
                        'id': 'hero-background'
                    },
                }) %}
                {{ heroImage|raw }}
            {% endif %}
            <div class="hero-content">
                <div class="container">
                    <div class="row">
                        <a href="/" class="col-12 text-center">
                            <img id="logo" class="mb-5" src="{{ asset('static/gfx/freiweg-logo.svg') }}" alt="FreiWeg Outdoor Activity Blog">
                        </a>
                    </div>
                </div>
            </div>
        </header>
        {% include 'layouts/includes/navigation-main.html.twig' %}

        <section id="intro" class="container-fluid spaced">
            <div class="container">
                <h1>Bitte keine Abkürzungen nutzen</h1>
                <p class="mt-3">
                    Es scheint, als hättest Du irgendwo die falsche Abbiegung erwischt. Hier geht es auf jeden Fall nicht weiter.<br>
                    Keine Sorge. Wir bringen Dich gerne zurück auf den passenden Weg!
                </p>
                <p class="mb-5">
                    Auf <a href="/">unserer Startseite</a> findest Du aktuelle Blogposts und beliebte Themen. 🙂
                </p>

                <h2>Oder entdecke gleich spannende Themen:</h2>
                <div class="row g-2 mt-3">
                    {% for collection in collections %}
                        {% include 'content/_general/collection_list_item.html.twig' with {'collection': collection} %}
                    {% endfor %}
                </div>
            </div>
        </section>
    </div>
{% endblock %}